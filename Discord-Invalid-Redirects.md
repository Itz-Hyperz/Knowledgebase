# Discord Redirects

All of my products that use Discord authentication have the same redirect URL! Just replace the `example.tld` with your domain.

```
https://example.tld/auth/discord/callback
http://example.tld/auth/discord/callback
```
